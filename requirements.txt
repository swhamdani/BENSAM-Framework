# BENSAM Framework Python Dependencies

pytest            # For automated testing
rich              # Optional: Rich CLI formatting for logs and reports
pandas            # Optional: For structured data manipulation if needed
python-dateutil   # Optional: For flexible datetime parsing
